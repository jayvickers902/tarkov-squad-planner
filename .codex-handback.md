## Handback

1. Verification

- Stage A gate: 49 files / 305 tests passed; build passed with 176 modules transformed.
- Final: 50 files / 311 tests passed; build passed with 178 modules transformed.
- The sandbox blocked Vite’s default `.vite-temp` writes, so both commands used the supported `--configLoader runner` option. No configuration files were changed.
- `securityContract.test.js` passed and remains unmodified.

2. Tests updated

- `myQuests.test.jsx` — verifies the collapsed zero state and manual-search focus.
- `myQuestsImportReceipt.test.jsx` — verifies undo returns to the collapsed zero state.
- `questOnboarding.test.jsx` — confirms background-sync guidance remains in `DesktopAppCard`.
- `welcome.test.js` — asserts the exact three-step setup data.
- `welcomeModal.test.jsx` — asserts three rendered setup steps.
- `appWelcome.test.jsx` — covers the shortened guide and active persistent navigation.
- `SyncStatusBar.test.jsx` — covers inline sync details without nested dialogs.
- Added `AppNav.test.jsx` — covers active state, conditional destinations, routing, and leave confirmation.

3. FIX 1 map labels

- `.map-btr-marker`: raised to `var(--fs-xs)` and enlarged from 26×18px to 34×22px.
- `.map-zone-label`: raised to `var(--fs-xs)` with 3×6px padding.
- `.map-offscreen-chevron-label`: raised to `var(--fs-xs)`; container padding/gap increased to 3×5px/3px.

4. FIX 2 border audit

No specific surface required reverting the brighter `--brd2`.

- `--brd` → `--brd2`: 5 interactive selectors/surfaces.
- `--brd2` → `--brd`: 6 decorative declarations/surfaces.

5. FIX 9 remaining raw glyphs

`⛺` is gone. Raw glyphs remain outside the scoped header/action replacements in:

`App.jsx`, `AdminKeyManager.jsx`, `BossPanel.jsx`, `CatchUp.jsx`, `EftLogImport.jsx`, `FindItems.jsx`, `Lobby.jsx`, `MapLeaflet.jsx`, `MyQuestPanel.jsx`, `MyQuests.jsx`, `QuestImportHub.jsx`, `QuestScanner.jsx`, `QuestSearch.jsx`, `RaidRail.jsx`, `RaidSettings.jsx`, `RaidView.jsx`, `RequiredItems.jsx`, `Room.jsx`, `StartRaidModal.jsx`, `SyncStatusBar.jsx`, and `TodoList.jsx`.

6. Stage B deviations

None. All Stage B fixes were implemented as specified. No commits or pushes were made. `supabase/`, `companion/`, package manifests, and the three legacy map files were left untouched; the pre-existing `supabase/.temp/cli-latest` change remains.